{
  "name": "inject-and-sirv",
  "version": "0.0.1",
  "scripts": {
    "start": "node inject.js && sirv dist_injected --port 5000 --cors --single --host"
  },
  "private": true,
  "dependencies": {
    "fs-extra": "^10.0.0",
    "klaw": "^3.0.0",
    "sirv-cli": "^3.0.1"
  },
  "engines": {
    "node": ">=18"
  }
}
